# Copy to `.env` and fill in from https://www.strava.com/settings/api
# Authorization Callback Domain on Strava must be: localhost
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Where the backend listens, and where it sends you back after OAuth.
PORT=4000
WEB_ORIGIN=http://localhost:5173
